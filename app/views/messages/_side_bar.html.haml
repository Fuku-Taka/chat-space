.side_bar
  .user-name
    .top-items
      %h3.name
        = current_user.name
      .icons
        .icon
          = link_to new_group_path do
            = icon('far', 'edit')
        .icon
          = link_to edit_user_path(current_user) do
            = icon('fas','cog')
  .group-list
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .group-name
            = group.name
          .group-message
            新しいメッセージ